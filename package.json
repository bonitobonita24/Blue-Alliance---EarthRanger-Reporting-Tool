{
  "name": "earthranger-reporting-tool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --test 'test/**/*.test.js'",
    "cache:backfill": "node scripts/backfill-patrol-cache.js",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "engines": {
    "node": ">=20"
  }
}
